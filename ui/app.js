function calculateB2BGst(amount, isExport){ if(isExport) return 0; return amount*0.18;}function getInvoiceTotal(baseAmount,isExport){const gst=calculateB2BGst(baseAmount,isExport);return baseAmount+gst;}document.addEventListener("DOMContentLoaded",()=>{const baseAmountInput=document.getElementById("baseAmount");const invoiceTypeSelect=document.getElementById("invoiceType");const resultEl=document.getElementById("result");const calculateBtn=document.getElementById("calculateBtn");calculateBtn.addEventListener("click",()=>{const base=parseFloat(baseAmountInput.value||"0");const isExport=invoiceTypeSelect.value==="export";const total=getInvoiceTotal(base,isExport);const label=isExport?"B2B Export":"B2B Domestic";resultEl.textContent=`Invoice Total (${label}): ${total.toFixed(2)}`;});});